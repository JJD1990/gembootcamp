<div class="card mb-3">
    <div class="card-header"><h1>Editing Lesson</h1></div>
        <div class="card-body">
            <%= render 'form', lesson: @lesson %>
        </div>
            <div class="card-footer">
                <%= link_to 'Show Lesson', course_lesson_path(@course, @lesson) %> |
                <%= link_to 'Back to Course', course_path(@lesson.course) %>
            </div>
</div>
